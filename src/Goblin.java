import java.awt.*;
import java.util.ArrayList;

public class Goblin {
    String GobUnicode = "\uD83D\uDC7A";
    int HP = (int) Math.ceil(Math.random() * 10);
    int STR = (int) Math.ceil(Math.random() * 10);
    int [] gobPosition = {4, 4};
}
